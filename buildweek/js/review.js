/*let svgs = document.querySelectorAll('svg')
let array = []
for (let el of svgs) {
    array.push(el)
    el.addEventListener('mouseover',(e) => {
        if (e < array[2]) {
            e.classList.add('path')
        }
    })
}*/